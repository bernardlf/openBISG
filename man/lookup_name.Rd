% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup.R
\name{lookup_name}
\alias{lookup_name}
\title{Look up a name in one of the bundled Census tables}
\usage{
lookup_name(
  name,
  table = c("first", "last", "first_sex", "first_extra", "last_extra")
)
}
\arguments{
\item{name}{A length-one character vector. \code{NA}, empty, or whitespace-only
inputs return \code{NULL}.}

\item{table}{One of \code{"first"}, \code{"last"}, \code{"first_sex"},
\code{"first_extra"}, or \code{"last_extra"}. The two \verb{_extra} tables are
the Rosenman, Olivella, and Imai (2023) NotInCensus2020 voter-file
subsets; they contain only names that are absent from the
corresponding Census table, so a single-table lookup against an
\verb{_extra} table only returns a hit for those rare names. For most
callers it is more convenient to set \code{include_extra = TRUE} on
\code{\link[=lookup_with_fallback]{lookup_with_fallback()}}, \code{\link[=lookup_compound_or_tokens]{lookup_compound_or_tokens()}},
\code{\link[=predict_race]{predict_race()}}, or \code{\link[=predict_names]{predict_names()}} than to call
\code{lookup_name()} directly with an \verb{_extra} table.}
}
\value{
\code{NULL} if no rule matched, otherwise a list with components:
\describe{
\item{probs}{Named numeric vector of group proportions for the matched
row.}
\item{matched_as}{The form of the name that hit the table.}
\item{rule}{Which cascade rule matched (\code{"exact"}, \code{"punctuation removed"}, \code{"punctuation and spaces removed"}, \code{"generational suffix removed"}, \code{"first segment of multi-part name"}, or \code{"second segment of multi-part name"}).}
\item{frequency}{The Census-published \code{FREQUENCY (COUNT)} for the
matched row.}
}
}
\description{
Applies the same wru-style normalization cascade as the bundled
\code{index.html} lookup page, returning the first table hit. The cascade is:
}
\details{
\enumerate{
\item \strong{exact} — \code{\link[=normalize_name]{normalize_name()}} then exact match;
\item \strong{punctuation removed} — drop non-alphanumerics except spaces;
\item \strong{punctuation and spaces removed} — also drop spaces;
\item \strong{generational suffix removed} (last names only) — strip JR, SR, II,
III, IV, JUNIOR, SENIOR, THIRD;
\item \strong{first / second segment of multi-part name} — split on hyphen, comma,
or space and try the first then the second piece.
}
}
\examples{
lookup_name("MICHAEL", table = "first")
lookup_name("PENA",    table = "last")
lookup_name("O'CONNOR", table = "last")    # "punctuation removed" rule
lookup_name("SMITH JR", table = "last")    # "generational suffix removed"
}
