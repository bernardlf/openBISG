% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup.R
\name{lookup_compound_or_tokens}
\alias{lookup_compound_or_tokens}
\title{Look up a field that may be a single compound name or several tokens}
\usage{
lookup_compound_or_tokens(
  tokens,
  tables = c("first", "last"),
  include_extra = FALSE
)
}
\arguments{
\item{tokens}{Character vector of name tokens (already tokenized).}

\item{tables}{Tables to try in order — see \code{\link[=lookup_with_fallback]{lookup_with_fallback()}}.}

\item{include_extra}{Forwarded to \code{\link[=lookup_with_fallback]{lookup_with_fallback()}} — when
\code{TRUE}, Census misses fall through to the Rosenman, Olivella, and Imai
(2023) NotInCensus2020 voter-file tables.}
}
\value{
Named list of hits/NULLs. When the whole-string match
succeeds, returns a single-element list keyed by the joined form;
otherwise returns one element per token, keyed by token. The
length of the result tells the caller how many evidence pieces to
feed into the Naive-Bayes combination.
}
\description{
Tries the entire input as a single name first — passing through the
\code{\link[=lookup_name]{lookup_name()}} cascade, which strips internal spaces and so will
catch the Census-collapsed compound forms (e.g. \code{"MARY ANN"} →
\code{MARYANN}, \code{"MARIA JOSE"} → \code{MARIAJOSE}). If the whole-string
lookup misses, falls back to per-token lookup with the same fallback
chain.
}
\details{
This matches \code{wru}'s "try whole, then split" behavior on a single
name field, but extended so that \strong{all} tokens of a multi-token
field are looked up (and combined later) when the compound form is
absent — \code{wru} only looks at the first two halves.
}
\examples{
## "MARIA JOSE" hits the MARIAJOSE row as one evidence piece.
lookup_compound_or_tokens(c("Maria", "Jose"))
## "JOHN SMITH" misses the joined form, so per-token lookup runs.
lookup_compound_or_tokens(c("John", "Smith"))
}
