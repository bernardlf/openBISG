% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup.R
\name{lookup_with_fallback}
\alias{lookup_with_fallback}
\title{Look up a name token in one or more tables, with optional fallback}
\usage{
lookup_with_fallback(name, tables = c("first", "last"), include_extra = FALSE)
}
\arguments{
\item{name}{Length-one character vector.}

\item{tables}{Character vector of table keys to try in order
(\code{"first"}, \code{"last"}, or \code{"first_sex"}). Default tries first then
last (the conventional given-name fallback).}

\item{include_extra}{If \code{TRUE}, after every Census table in \code{tables}
has missed, fall back to the corresponding Rosenman, Olivella, and Imai
(2023) NotInCensus2020 table (\code{first_extra} / \code{last_extra}). The
Rosenman tables only cover names that are absent from Census 2020,
so a Census-table hit always wins over a Rosenman hit. Default
\code{FALSE}.}
}
\value{
\code{NULL} if every table missed, otherwise the \code{\link[=lookup_name]{lookup_name()}}
result with \code{source} and \code{dataset} appended.
}
\description{
Tries each table in order, returning the first hit. Each table is
looked up with the full normalization cascade in \code{\link[=lookup_name]{lookup_name()}}.
The returned hit carries two extra elements: \code{source} naming the
Census-style table key (\code{"first"}, \code{"last"}, or \code{"first_sex"}) and
\code{dataset} naming the underlying source (\code{"census"} for the bundled
2020 Census tables or \code{"rosenman"} for the Rosenman, Olivella, and Imai
(2023) NotInCensus2020 voter-file additions).
}
\examples{
lookup_with_fallback("MICHAEL", tables = c("first", "last"))
lookup_with_fallback("AABIDA",  tables = c("first", "last"),
                      include_extra = TRUE)
}
