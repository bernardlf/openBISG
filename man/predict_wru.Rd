% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_wru.R
\name{predict_wru}
\alias{predict_wru}
\title{wru-style BISG from the bundled geography tables}
\usage{
predict_wru(
  data,
  geography_type = c("vap", "cvap", "pop"),
  include_extra = FALSE,
  geo_smooth = 0,
  progress = TRUE,
  n_cores = 1L
)
}
\arguments{
\item{data}{A data.frame with a surname column named \code{last} or
\code{surname} (case-insensitive; \code{last} wins if both are present) and
one geography column named \code{block}, \code{block_group}, or \code{tract}
(most specific wins). Geography IDs may carry the Census Summary
File prefixes; they are normalized as in \code{\link[=geo_prior]{geo_prior()}}.}

\item{geography_type}{\code{"vap"} (default) — 2020 Decennial voting-age
population — \code{"cvap"} (citizen voting-age population;
block-group / tract only), or \code{"pop"} — total population of all
ages, \strong{wru's exact default basis} (P.L. 94-171 Table P2), which
requires the separately built \verb{geo_*_pop} tables (see
\code{\link[=geo_prior]{geo_prior()}}) and makes the replication apples-to-apples.}

\item{include_extra}{When \code{TRUE}, surnames absent from the Census
2020 list fall back to the Rosenman, Olivella, and Imai (2023)
voter-file table (collapsed to the five categories), as in
\code{\link[=predict_names]{predict_names()}}. Default \code{FALSE} — the Census list only, as in
wru.}

\item{geo_smooth}{Pseudo-count smoothing of the geography component
toward the table-wide marginal, as in \code{\link[=predict_demog]{predict_demog()}}. Default
\code{0} for wru fidelity (raw counts).}

\item{progress, n_cores}{Forwarded to \code{\link[=predict_demog]{predict_demog()}}.}
}
\value{
A data.frame with \code{nrow(data)} rows and columns \code{p_whi},
\code{p_bla}, \code{p_his}, \code{p_asi}, \code{p_oth}, summing to 1 per row when any
evidence matched and \code{NA_real_} otherwise. Corresponds to wru's
\code{pred.whi} ... \code{pred.oth}.
}
\description{
Replicates the estimation of the \code{wru} package's standard BISG
(Imai and Khanna 2016) — surname times Census geography, in wru's
five racial categories — without downloading Census data: the
geography component comes from the bundled openBISG tables at the
Census Block, Block Group, or Tract level.
}
\details{
\code{wru} computes \eqn{P(R \mid G, S) \propto P(G \mid R) P(R \mid S)},
with \eqn{P(G \mid R)} taken from Census counts downloaded for the
voter's state at the chosen geography level. Since
\eqn{P(G \mid R) = P(R \mid G) P(G) / P(R)}, that is algebraically
the openBISG fold \eqn{P(R \mid S) P(R \mid G) / P(R)} with the
state's racial composition — at the same level and population basis
— as the marginal \eqn{P(R)}. \code{predict_wru()} runs exactly that
fold, state by state, entirely from the bundled tables.

Fidelity to \code{wru}:
\itemize{
\item The six openBISG groups are collapsed to wru's five
\strong{before} the fold, as wru collapses the Census surname list:
\code{whi} = white, \code{bla} = black, \code{his} = hispanic, \code{asi} = aapi,
\code{oth} = aian + nh_multi.
\item Raw counts are folded (\code{geo_smooth = 0} by default), so — as
in wru — a zero Census cell forces that group's posterior to
zero. Pass \code{geo_smooth > 0} to depart from wru here.
\item There is no block-group fallback and no \code{block_shrink}:
rows whose geography misses the table carry the surname-only
posterior (reported once per call via \code{\link[=message]{message()}}).
\item Rows whose surname finds no dictionary entry carry the
geography-only posterior, wru's \code{impute.missing} analog.
}

Known departures, all documented consequences of using the bundled
data: the default population basis is 2020 voting-age population
rather than wru's all-ages counts — pass \code{geography_type = "pop"}
(with the \verb{geo_*_pop} tables built) to match wru's basis exactly;
surnames are cleaned and compound-matched by openBISG's cascade
rather than wru's; and no age / sex / party conditioning is applied
(wru's basic BISG mode).
}
\examples{
df <- data.frame(last  = c("Garcia", "Smith"),
                 block = rep(geo_block_vap$geoid[1], 2))
predict_wru(df, progress = FALSE)
}
\references{
Imai, K. and Khanna, K. (2016). Improving Ecological
Inference by Predicting Individual Ethnicity from Voter
Registration Records. Political Analysis 24(2), 263–272.
}
\seealso{
\code{\link[=predict_demog]{predict_demog()}} for the full six-category openBISG
estimator, \code{\link[=geo_prior]{geo_prior()}} for the bundled geography tables.
}
