% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_race}
\alias{predict_race}
\title{Predict race / Hispanic-origin (and sex) probabilities from one or
more given names and surnames}
\usage{
predict_race(
  first = NULL,
  middle = NULL,
  last = NULL,
  maiden = NULL,
  include_extra = FALSE,
  zcta = NULL,
  tract = NULL,
  block_group = NULL,
  block = NULL,
  geography_type = c("cvap", "vap", "pop"),
  geo_smooth = 1,
  block_fallback = TRUE,
  block_shrink = 10,
  geography_probs = NULL
)
}
\arguments{
\item{first}{Given names (whitespace-separated string or vector).}

\item{middle}{Middle names. Looked up like \code{first}.}

\item{last}{Surnames. Tokenized; per-token cross-table fallback.}

\item{maiden}{Maiden name. Same handling as \code{last}. When provided
(non-empty), takes the place of \code{last} in the combined estimate.}

\item{include_extra}{If \code{TRUE}, names that miss every Census 2020
table fall back to the Rosenman, Olivella, and Imai (2023)
NotInCensus2020 voter-file tables (\code{first_names_extra} /
\code{last_names_extra}) before being recorded as a miss. The Rosenman
tables only contain names absent from Census 2020, so a Census hit
always takes precedence. The Rosenman source ships only 5 race
columns (white, black, AAPI, OTHER, Hispanic, where OTHER lumps
AIAN, two-or-more, and other); for combination with the rest of
the package, OTHER is split between AIAN and \code{nh_multi}
proportionally to the Census prior at build time. Default \code{FALSE}.
Sex prediction is unaffected — the Rosenman dataset has no sex
information, so the \code{first_names_sex} table is the only source.}

\item{zcta, tract, block_group, block}{Optional geography identifier. At
most one may be supplied. When given, the name posterior is folded
together with the geographic prior under conditional independence
given race (BISG / BIFSG):
\deqn{P(R | \mathrm{name}, G) \propto P(R | \mathrm{name})\,P(R | G)\,/\,P(R)}.
See \code{\link[=geo_prior]{geo_prior()}} for accepted formats and the bundled tables.
\code{block} takes a 15-digit 2020 Census Block GEOID; block-level data
are VAP only, and a block that is not among the populated 2020
blocks falls back to its parent block group (see \code{\link[=geo_prior]{geo_prior()}},
whose messages this function inherits).}

\item{geography_type}{\code{"cvap"} (default), \code{"vap"}, or \code{"pop"} —
picks which bundled geography table feeds the prior when \code{zcta} /
\code{tract} / \code{block_group} / \code{block} is supplied. There is no
block-level CVAP table, so \code{block} with \code{"cvap"} uses the block's
parent block group. \code{"pop"} is the total population of all ages
(P.L. 94-171 Table P2, the basis used by the \code{wru} package);
it covers tract / block group / block only and requires the
separately built \verb{geo_*_pop} tables — see \code{\link[=geo_prior]{geo_prior()}}.}

\item{geo_smooth}{Pseudo-count, in people, used to shrink the bundled
geographic prior toward the national marginal of the same table
before it is folded in (default \code{1}). This keeps an exact zero in
\code{P(R | G)} — common at block-group scale, where 49\% of CVAP rows
estimate no Asian / NHPI citizens — from forcing that group's
posterior to zero regardless of the name evidence. See
\code{\link[=geo_prior]{geo_prior()}} for the formula. Set to \code{0} to fold in the published
shares unchanged. Ignored when \code{geography_probs} is supplied: a
caller-supplied prior is used exactly as given.}

\item{block_fallback}{Forwarded to \code{\link[=geo_prior]{geo_prior()}}: when \code{TRUE}
(default), a \code{block} GEOID not among the populated 2020 blocks
falls back to its parent block group's row; when \code{FALSE} the
geography lookup is recorded as not found. Only consulted for
\code{block} lookups.}

\item{block_shrink}{Forwarded to \code{\link[=geo_prior]{geo_prior()}}: pseudo-count, in
people, of the parent block group's composition blended into a
block's VAP counts before normalizing (default \code{10}; \code{0} restores
the raw 0.7.0 block counts). Only consulted for \code{block} lookups.}

\item{geography_probs}{Optional length-6 named numeric vector of
\code{P(R | G)} (in \code{\link[=race_groups]{race_groups()}} order). Lets callers plug in a
prior from any external source without going through
\code{\link[=geo_prior]{geo_prior()}} — e.g. a tract-level prior built from a different
ACS vintage, or a tabulated school-attendance-area prior.
Mutually exclusive with \code{zcta} / \code{tract} / \code{block_group}.}
}
\value{
A list with:
\describe{
\item{groups, group_labels}{Race-group keys and labels.}
\item{tokens}{\code{list(first, middle, last, maiden)}. Each is a
named list (names = the lookup keys, which are the joined
compound when matched as one token, or the individual tokens
otherwise) of hits or \code{NULL}.}
\item{surname_used}{\code{"maiden"} if any maiden token was supplied,
\code{"last"} if last tokens were supplied without maiden, or
\code{NULL} if neither.}
\item{combined}{\code{list(probs = ..., n = k)} — or \code{NULL} if no
evidence matched.}
\item{geography}{\code{NULL} when no geography was supplied,
otherwise a list describing the lookup: \code{level}, \code{type}, \code{key},
\code{total}, \code{source}, \code{found}, the \code{geo_smooth} pseudo-count that
was applied, the (smoothed) \code{probs} fed to the fold, and
\code{combined} — the BISG posterior \code{P(R | name, G)}. When a
\code{block} lookup was rerouted to its parent block group, \code{level}
is \code{"block_group"} and \code{fallback_from} is \code{"block"}.}
\item{sex}{\code{list(probs = ..., tokens, n)} — or \code{NULL} if no
first-name input was provided.}
}
}
\description{
Each input field is tokenized on whitespace (a length-one string is
split; a character vector is treated as already-tokenized).
}
\details{
\strong{Given-name fields (\code{first}, \code{middle})} use a "compound first,
tokens otherwise" cascade implemented by \code{\link[=lookup_compound_or_tokens]{lookup_compound_or_tokens()}}:
the entire field is first looked up as a single name (the cascade in
\code{\link[=lookup_name]{lookup_name()}} strips internal spaces, so \code{"MARY ANN"} → \code{MARYANN},
\code{"MARIA JOSE"} → \code{MARIAJOSE}); only if that misses does it fall back
to per-token lookup. This preserves the way the Census tabulated
compound first names — which carry their own (often very different)
race and sex distributions from the parts.

\strong{Surname fields (\code{last}, \code{maiden})} are looked up per-token (no
compound retry). When \code{maiden} is non-empty its tokens \strong{replace}
the \code{last} tokens in the combined estimate; the \code{last} lookups are
still reported under \verb{$tokens$last} but \verb{$surname_used} is set to
\code{"maiden"}.

Each given-name token / lookup falls back to the last-name table on
miss; each surname token falls back to the first-name table. Tokens
absent from both tables are reported but excluded from the combined
estimate.

Combination across the \code{k} matched evidence pieces uses Naive Bayes:

\verb{P(R | n1, ..., nk) ∝ Π P(R | n_i) / P(R)^(k - 1)}

under the assumption that all \code{k} name tokens are conditionally
independent given race. With \code{k = 1} (e.g. a single first name, or a
compound that matched as one token) the formula reduces to
\code{P(R | n1)} — the per-name proportion read directly from the
dictionary.

Sex is predicted from the \strong{first-name field only} (middle names
are excluded, see \code{\link[=predict_sex]{predict_sex()}}), using the same compound-first
cascade against the sex table — so
\code{predict_race(first = "Maria Jose")} reads the sex probability
directly from the \code{MARIAJOSE} row instead of (mis-) combining the
male \code{JOSE} row with the female \code{MARIA} row.
}
\examples{
## Single name + surname.
predict_race(first = "Maria", last = "Garcia")

## Compound first reads MARIAJOSE directly.
predict_race(first = "Maria Jose", last = "Garcia")

## Maiden replaces last in the combined estimate.
predict_race(first = "Maria", last = "Smith", maiden = "Garcia")

## BISG: fold a ZIP-level prior into the name posterior.
predict_race(first = "Maria", last = "Garcia", zcta = "30307")

## Geography only: collapses to P(R | G).
predict_race(zcta = "00601")
}
\seealso{
\code{\link[=lookup_name]{lookup_name()}}, \code{\link[=lookup_with_fallback]{lookup_with_fallback()}},
\code{\link[=lookup_compound_or_tokens]{lookup_compound_or_tokens()}}, \code{\link[=tokenize_names]{tokenize_names()}}, \code{\link[=predict_sex]{predict_sex()}}.
}
