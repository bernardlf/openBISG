% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_names}
\alias{predict_names}
\title{Vectorized BISG / BIFSG prediction over a data frame}
\usage{
predict_names(
  data,
  include_extra = FALSE,
  geography_type = c("cvap", "vap"),
  geo_smooth = 1,
  progress = TRUE,
  n_cores = 1L
)
}
\arguments{
\item{data}{A data frame with any subset of the recognized columns
listed above.}

\item{include_extra}{Forwarded to \code{\link[=predict_race]{predict_race()}} — when \code{TRUE},
names absent from Census 2020 are looked up against the
Rosenman, Olivella, and Imai (2023) NotInCensus2020 voter-file
tables. Default \code{FALSE}.}

\item{geography_type}{\code{"cvap"} (default) or \code{"vap"} — selects the
bundled geography table used when a geography column is detected.}

\item{geo_smooth}{Forwarded to \code{\link[=predict_race]{predict_race()}} — the pseudo-count
used to shrink the geographic prior toward the national marginal
before folding, which keeps sampling zeros in \code{P(R | G)} from
zeroing out a group the names point to. Default \code{1}; \code{0} disables.}

\item{progress}{If \code{TRUE} (default), prints a one-line text progress
bar to \code{stderr} showing percent complete, elapsed time, and an
estimated time remaining. Pass \code{FALSE} to suppress (e.g. inside
non-interactive scripts or when capturing output). The bar is only
drawn for the serial path (\code{n_cores == 1L}); in the parallel path
it is replaced by a single start / finish status line.}

\item{n_cores}{Number of worker processes to use. Default \code{1L}
(serial). When \code{n_cores > 1L}, rows are split into \code{n_cores}
chunks and processed via \code{parallel::mclapply} (fork-based, so
the large bundled lookup tables are shared via copy-on-write —
no per-worker export cost). On Windows fork is unavailable, so
values above 1 are ignored and rows are processed serially.
Capped at \code{min(n_cores, parallel::detectCores(), nrow(data))}.}
}
\value{
A data frame with \code{nrow(data)} rows and 7 columns:
\code{p_white}, \code{p_black}, \code{p_aian}, \code{p_aapi}, \code{p_nh_multi},
\code{p_hispanic} (race probabilities, summing to 1 per row when
matched), and \code{p_female} (\code{P(female | first)}; \code{1 - p_female}
gives \code{P(male)}). All cells are \code{NA_real_} for rows with no signal.
}
\description{
Auto-detects which input columns are present in \code{data} and returns a
fixed-shape data frame of per-row probabilities. Recognized columns
(any subset, named exactly):
\itemize{
\item name fields: \code{first}, \code{middle}, \code{last}, \code{maiden}
\item geography fields: \code{zcta}, \code{tract}, \code{block_group}
}
If multiple geography columns are present the most specific one is
used (\code{block_group} > \code{tract} > \code{zcta}). If no recognized columns are
present, an error is raised.
}
\details{
Per-row behavior matches \code{\link[=predict_race]{predict_race()}} exactly: compound-first
cascade for given-name fields; per-token cascade for surname fields;
maiden tokens replace last in the combined estimate; sex computed
from the \code{first} field only. Race probabilities hold the
BISG-combined posterior \code{P(R | name, G)} when a geography column is
present and matched, falling back to the name-only posterior
otherwise. Rows where nothing matched contain \code{NA_real_}.

For a single per-call prediction or to inspect the per-token / sex
detail, use \code{\link[=predict_race]{predict_race()}} and \code{\link[=predict_sex]{predict_sex()}}.
}
\examples{
df <- data.frame(
  first = c("Maria",  "John",  "Mary Ann"),
  last  = c("Garcia", "Smith", "Johnson"),
  zcta  = c("30307",  "10001", "94110"),
  stringsAsFactors = FALSE
)
predict_names(df)

\dontrun{
## Parallelize a large data frame across 8 cores (Unix / macOS).
predict_names(big_df, n_cores = 8L)
}
}
\seealso{
\code{\link[=predict_race]{predict_race()}}, \code{\link[=predict_sex]{predict_sex()}}, \code{\link[=geo_prior]{geo_prior()}}.
}
