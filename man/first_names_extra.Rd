% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{first_names_extra}
\alias{first_names_extra}
\title{Rosenman, Olivella, and Imai (2023) first-name additions not in Census 2020}
\format{
A data frame with 85,213 rows and 8 columns:
\describe{
\item{name}{Uppercase, NFD-stripped first name (length-one ASCII).}
\item{frequency}{\code{NA_integer_} — Rosenman publishes only the
conditional probabilities, not name-level frequencies.}
\item{white,black,aian,aapi,nh_multi,hispanic}{Proportion of
people with this first name belonging to each of the six
race / Hispanic-origin groups. Rows sum to 1.}
}
Two attributes are attached:
\describe{
\item{prior}{The Census-2020 first-name prior, copied from
\link{first_names} (Rosenman has no frequencies of its own to derive
a prior from, and the Naive-Bayes formula uses the
\link{last_names} prior throughout regardless).}
\item{groups}{Length-6 character vector of column keys, in
\code{\link[=race_groups]{race_groups()}} order.}
}
}
\source{
Rosenman, E. T. R., Olivella, S., & Imai, K. (2023). Race
and ethnicity data for first, middle, and surnames. \emph{Scientific
Data} 10, 299.
\url{https://doi.org/10.7910/DVN/YL2OXB} (Harvard Dataverse, V1).
}
\usage{
first_names_extra
}
\description{
Voter-file first-name race / Hispanic-origin probabilities for the
85,213 names that appear in \code{first_nameRaceProbs.rData} but \strong{not}
in \link{first_names}. Used as a fallback when \code{\link[=predict_race]{predict_race()}} /
\code{\link[=predict_names]{predict_names()}} / \code{\link[=lookup_with_fallback]{lookup_with_fallback()}} are called with
\code{include_extra = TRUE} and every Census 2020 table missed.
}
\details{
Rosenman ships only 5 race columns — white, black, AAPI, OTHER
(which lumps AIAN, two-or-more, and other), and Hispanic. To stay
compatible with the 6-column Census tables and the Naive-Bayes
combination unchanged, the OTHER bucket is split between \code{aian} and
\code{nh_multi} proportionally to the Census prior at build time:
\code{aian = OTHER * P(aian) / (P(aian) + P(nh_multi))} and
\code{nh_multi = OTHER * P(nh_multi) / (P(aian) + P(nh_multi))}.
}
\seealso{
\link{first_names}, \link{last_names_extra}.
}
\keyword{datasets}
